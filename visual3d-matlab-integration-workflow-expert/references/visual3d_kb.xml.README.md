# visual3d_kb.xml

The full `visual3d_kb.xml` file is intentionally managed as a local knowledge-base artifact because it is large.

Expected location for full offline usage:

- `visual3d-matlab-integration-workflow-expert/references/visual3d_kb.xml`

Source used during skill build:

- `D:/Research work/Code/Biomechanics/visual3d_kb.xml`

If needed, copy the XML into this folder before running full extraction workflows.
